# MongoDB Connection String
# For MongoDB Atlas (Cloud - Recommended since you don't have local MongoDB):
# 1. Create a free account at https://www.mongodb.com/cloud/atlas/register
# 2. Create a cluster (free tier available)
# 3. Get your connection string and replace below
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-url>/<database-name>?retryWrites=true&w=majority

# For Local MongoDB (if you install it later):
# MONGODB_URI=mongodb://localhost:27017/kadlekai-parishe

# Server Configuration
PORT=5000
NODE_ENV=development

# CORS - Frontend URL
CLIENT_URL=http://localhost:5173
